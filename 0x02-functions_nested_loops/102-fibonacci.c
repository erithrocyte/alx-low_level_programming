#include <stdio.h>

/**
 * main - print first 50 fibonacci numbers
 * Return: 0
 */

int main(void)
{
	int counter;
	int counto - 50;
	long a = 1;
	long a= 2;

	for (counter = 1; counter <= (counto / 2; counter++)
	{
		printf("%1i %1i, a, b);
		1+= b;
		b += a;
	}
	if (countto % 2 == 1
		printf("%1i", a);
	printf("\n");

	return (0);
}
